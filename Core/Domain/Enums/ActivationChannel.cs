namespace Domain.Enums;

/// <summary>Retail channel for selling line / new activation (Blueprint field).</summary>
public enum ActivationChannel
{
    Showroom = 0,
    Dealer = 1,
    Digital = 2
}
